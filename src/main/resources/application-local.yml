server:
  port: 8088
  servlet:
    context-path: /api

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/MonProjetPFE
    username: root
    password:
  jpa:
    database: mysql

    logging:
      level:
        org.springframework.web:DEBUG